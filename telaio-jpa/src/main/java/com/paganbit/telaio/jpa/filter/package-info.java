/**
 * JSON-aware filtering support: bridges the JSON wire names clients send in filter queries to the
 * underlying Java property names the Turkraft Spring Filter library resolves against the JPA metamodel.
 *
 * @author Marco Pagan
 * @since 1.0.0
 */
@NullMarked
package com.paganbit.telaio.jpa.filter;

import org.jspecify.annotations.NullMarked;
